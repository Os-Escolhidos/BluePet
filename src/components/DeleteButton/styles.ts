import { StyleSheet } from 'react-native'

export const DeleteButton = StyleSheet.create({
    Button: {
        backgroundColor:"red",
        borderRadius: 8,
        textAlign: 'center',
        marginTop: 40,
        width: 220
    },
})